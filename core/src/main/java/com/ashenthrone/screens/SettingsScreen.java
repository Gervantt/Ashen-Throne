package com.ashenthrone.screens;

import com.ashenthrone.core.AshenThroneGame;

/**
 * Settings screen stub (AT-013).
 *
 * TODO: AT-017 — implement display mode toggle (fullscreen / 1280x720) and
 * read-only key bindings list. Persist via libGDX Preferences.
 */
public class SettingsScreen extends BaseScreen {

    public SettingsScreen(AshenThroneGame game) {
        super(game);
    }
}
